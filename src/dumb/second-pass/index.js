import getScore from '../getScore.js'
import byPattern from '../byPattern.js'
import getSafer from './get-safer.js'

const delta = function (arr) {
  let [a, b] = arr
  for (let i = 0; i < a.length; i += 1) {

  }
}


const secondPass = function (pairs, res) {
  // pairs.forEach(arr => {
  //   let [a, b] = arr
  //   // console.log(b.startsWith(a))
  // })
  // let { score, wrong } = getScore(pairs, res)
  // let size = 4
  // let kinds = byPattern(wrong, size)
  // kinds.forEach(obj => {
  //   // console.log(obj)
  //   // let to = getSafer(obj.list)
  //   // console.log(to)
  // })
  return res
}
export default secondPass